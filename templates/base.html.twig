<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
  </head>
  
  <body>
    <div id="app">
      <div class="notification">
        <div 
          v-for="(notificationItem, notificationIndex) in notifications"
          :key="notificationItem.id"
          class="notification__item"
        >
          <span>([[ notificationItem.id ]]) [[ notificationItem.type ]] - [[ notificationItem.message ]]</span>
          <button @click="removeNotification(notificationIndex)">close</button>
        </div>
      </div>

      {% block body %}
      {% endblock %}
    </div>

    {{ encore_entry_script_tags('app') }}

    {% block javascripts %}{% endblock %}

    <script>
      window.addEventListener('load', function () {
        AppMain.mount("#app");
        console.log(document.querySelector('#app'))
      })
    </script>
  </body>
</html>
